# zdotdir

Personal Zsh configuration using `ZDOTDIR`.

To use it, clone this repo and create symlinks from your home directory:

```sh
ln -s ~/.config/zsh/.zshenv ~/.zshenv
ln -s ~/.config/zsh/.zshrc ~/.zshrc
```

